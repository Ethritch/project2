@{
}

@model List<Appointment>

<h1>Appointments</h1>

<table class="table table-bordered table-striped">
    
    <thead>
        <tr>
            <td>Group Name</td>
            <td>Group Size</td>
            <td>Appointment</td>
            <td>Email</td>
            <td>Phone Number</td>
        </tr>
    </thead>

    <tbody>
        @foreach (var x in Model)
        {
        <tr>
            <td>@x.GroupName</td>
            <td>@x.GroupSize</td>
            <td>@x.Time.AppointmentTime</td>
            <td>@x.Email</td>
            <td>@x.Phone</td>

            <td><a>Edit</a></td>
            <td><a>Delete</a></td>
        </tr>
        }
    </tbody>

</table>